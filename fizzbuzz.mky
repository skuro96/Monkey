let fizzbuzz = fn(n) {
	let i = 0;
	while (i < n) {
		let str = "";

		if (i % 3 == 0) {
			str = str + "Fizz";
		}
		if (i % 5 == 0) {
			str = str + "Buzz";
		}

		if (len(str) > 0) {
			puts(str);
		} else {
			puts(i);
		}

		++i;
	}
}

fizzbuzz(100);